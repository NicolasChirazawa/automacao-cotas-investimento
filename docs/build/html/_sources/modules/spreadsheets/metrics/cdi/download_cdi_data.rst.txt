.. _download_cdi:

Download CDI Data
=================

A request is made to the Ipeadata API to retrieve historical CDI data (date and rate).  
The response is then processed and compiled into a ``.csv`` file.

.. warning::
   This section does not describe low-level implementation details. It focuses only on the main components and on aspects that may cause confusion.